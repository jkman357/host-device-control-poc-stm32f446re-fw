# Protocol Test Vectors

These files are synchronized from the authoritative system repository.

Both implementations shall encode and decode each vector byte-for-byte, reject a controlled CRC corruption,
preserve request sequences in direct responses, and record the tested Protocol file SHA-256.
